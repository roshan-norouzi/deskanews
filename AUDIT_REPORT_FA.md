# گزارش نهایی ممیزی معماری، امنیت و آمادگی عملیاتی DESKA ERP

تاریخ آخرین راستی‌آزمایی: ۲۰۲۶-۰۸-۳۰  
دامنه: monorepo، API، Web، Prisma/PostgreSQL، احراز هویت، چندسازمانی، نشر هوشمند، فایل‌ها، Docker، اسکریپت‌های Windows و CI

## جمع‌بندی مدیریتی

DESKA ERP یک Modular Monolith چندسازمانی با NestJS، Next.js و PostgreSQL است. ساختار فعلی برای ادامه توسعه و ورود به محیط staging مناسب است. مشکلات اثبات‌شده با ریسک بالا در جداسازی سازمان‌ها، مدیریت نشست، SSRF، رقابت هم‌زمان عملیات احراز هویت، یکپارچگی دیتابیس، نگهداری فایل و تنظیمات production اصلاح شده‌اند.

نتیجه واقعی کنترل نهایی:

- TypeScript در تمام workspaceها بدون خطا بررسی شد.
- هر ۲۳ تست API و بازگشت امنیتی موفق شدند.
- schema معتبر و اختلاف دیتابیس جاری با migrationها صفر است.
- ممیزی ۲۹ جدول سازمان‌محور هیچ orphan یا رابطه بین‌سازمانی ناسازگاری پیدا نکرد.
- ساخت production در API و Web موفق و هر ۳۲ مسیر Next.js تولید شد.
- lint دارای صفر خطا و ۹ هشدار غیرمسدودکننده مربوط به تصویرهای پویا است.
- ممیزی وابستگی‌های production در همه سطوح صفر آسیب‌پذیری شناخته‌شده گزارش کرد.
- پشته واقعی توسعه شامل PostgreSQL، API و Web با Hot Reload بالا آمد و smoke test زنده به‌طور کامل موفق شد.
- سلامت API، صفحه ورود، نشست HttpOnly، `/auth/me`، ماژول‌های هسته، CSRF، مخاطبین، اسناد، تقویم، کارمندان و داشبورد در اجرای زنده تأیید شدند.

این نتیجه به معنی «آماده staging» است، نه تأیید بی‌قیدوشرط production. تعویض secretها، تمرین restore و آزمون integrationهای خارجی با credential واقعی همچنان پیش‌شرط انتشار نهایی‌اند.

## معماری شناسایی‌شده

| بخش | وضعیت فعلی |
|---|---|
| Monorepo | pnpm workspace با package مشترک `@deska/shared` |
| Backend | NestJS 11، Prisma 6.19، PostgreSQL 16، REST API |
| Frontend | Next.js 15.5، React 19، TypeScript و رابط RTL |
| احراز هویت | access/refresh JWT، کوکی HttpOnly، rotation و ابطال فوری نشست |
| چندسازمانی | Tenant، عضویت، نقش و permission وابسته به سازمان فعال |
| ماژول‌های هسته | مخاطبین، اسناد، تقویم و کارمندان؛ همیشه فعال |
| ماژول‌های اختیاری | پروژه/تسک و نشر هوشمند بر اساس پلن و تنظیم سازمان |
| داده | PostgreSQL با ۳۵ migration و ۲۹ جدول tenant-scoped |
| فایل | ذخیره محلی محصور در پوشه سازمان؛ مناسب تک‌گره |
| زمان‌بندی | scheduler و قفل‌های درون‌پردازه‌ای؛ مناسب یک replica |
| استقرار توسعه | Docker برای PostgreSQL و فرایندهای Hot Reload برای API/Web |
| CI | PostgreSQL واقعی، migrate، drift check، ممیزی tenant، typecheck، lint، test و build |

## اصلاحات امنیتی و فنی انجام‌شده

### احراز هویت و نشست

- مرورگر دیگر access token یا refresh token را در `localStorage` نگه نمی‌دارد؛ کلیدهای نسخه‌های قدیمی فقط پاک‌سازی می‌شوند.
- access و refresh در کوکی‌های `HttpOnly`، با `SameSite=Lax` و `Secure` در production قرار می‌گیرند.
- Bearer token برای کلاینت‌های API سازگار باقی مانده است، اما Web از نشست کوکی استفاده می‌کند.
- refresh token به‌صورت hash ذخیره و در rotation به‌شکل اتمیک مصرف می‌شود.
- تغییر رمز، reset رمز، logout-all و غیرفعال‌سازی حساب، نشست‌های قبلی را فوراً نامعتبر می‌کنند.
- شمارنده تلاش ناموفق ورود و مصرف reset token در برابر درخواست‌های هم‌زمان اتمیک شدند.
- endpointهای حساس احراز هویت rate limit اختصاصی دارند.
- درخواست تغییردهنده مبتنی بر کوکی فقط از Origin مجاز پذیرفته می‌شود؛ آزمون زنده درخواست Cross-Site با پاسخ 403 موفق بود.

### جداسازی سازمان‌ها و RBAC

- `TenantGuard` وجود و فعال‌بودن سازمان را حتی برای super-admin کنترل می‌کند.
- permissionها فقط از عضویت و نقش همان سازمان فعال استخراج می‌شوند.
- اعلان‌ها هنگام فهرست‌کردن و تغییر وضعیت هم به `tenantId` و هم `userId` محدودند.
- شناسه سازمان در مسیر با context فعال تطبیق داده می‌شود.
- مدیر پلتفرم نمی‌تواند مالک آخر یک سازمان فعال را بدون رفع مالکیت غیرفعال کند.
- کارمندان به هسته منتقل شده‌اند؛ مسیرهای runtime ماژول HR حذف و `/employees` مرجع واحد شده است.
- کاربر پلتفرم، مالکیت سازمان و عضویت کارمندی در مدل‌های مستقل قابل مشاهده و مدیریت‌اند.

### SSRF و اتصال سرویس‌های بیرونی

- IPهای خصوصی، loopback، link-local، metadata و نمایش‌های خاص IPv4/IPv6 از جمله IPv4-mapped IPv6 رد می‌شوند.
- همه پاسخ‌های DNS بررسی می‌شوند؛ وجود حتی یک IP غیرعمومی باعث رد میزبان می‌شود.
- socket به IP اعتبارسنجی‌شده pin می‌شود و Host/SNI اصلی برای HTTPS حفظ می‌گردد.
- هر redirect فید دوباره اعتبارسنجی می‌شود.
- درخواست‌های credentialed به GapGPT، WordPress و Telegram Worker redirect را دنبال نمی‌کنند تا secret به مقصد دیگری نشت نکند.
- URLهای تنظیم‌شدنی در production باید HTTPS باشند؛ localhost فقط در development پذیرفته می‌شود.
- پاسخ، timeout و سقف حجم برای خواندن منبع و درخواست‌های خروجی کنترل می‌شود.

### دیتابیس و یکپارچگی داده

- کلید خارجی Tenant برای ۲۵ مدل تجاری اضافه شد و حذف Tenant به‌جای cascade مخرب با `RESTRICT` محافظت می‌شود؛ AuditLog تنها استثنای `SET NULL` است.
- migrationهای ابطال نشست و یکپارچگی Tenant اعمال شدند.
- ابزار read-only با دستور `pnpm db:audit` orphanها و رابطه‌های بین‌سازمانی شناخته‌شده را بررسی می‌کند.
- همین ممیزی به CI اضافه شد و بعد از migrate اجرا می‌شود.
- یک پوشه سند خالی و orphan مربوط به tenant قدیمی `default` پس از اثبات خالی‌بودن و وجود رکورد صحیح جایگزین حذف شد. بازیابی آن فقط از backup دیتابیس ممکن است، هرچند هیچ فایل یا فرزندی نداشت.
- پس از پاک‌سازی، ممیزی ۲۹ جدول با `issues: []` پایان یافت.
- بازخوانی فهرست اعضای سازمان دیگر در هر GET همه کارمندان را sync نمی‌کند؛ reconciliation فقط هنگام مشاهده عضویت فاقد رکورد کارمند انجام می‌شود.

### فایل، تنظیمات و خطا

- مسیر فایل اسناد به ریشه سازمان محدود، نام فایل روی دیسک تصادفی و نوشتن فایل انحصاری شده است.
- اگر ثبت دیتابیس شکست بخورد فایل جدید پاک می‌شود؛ هنگام حذف، رکورد دیتابیس مقدم است و پاک‌سازی دیسک بعد از آن انجام می‌شود.
- خروج مسیر، فایل گمشده و مسیر ناامن پاسخ کنترل‌شده می‌گیرند.
- secretهای تنظیمات در production بدون `SETTINGS_ENCRYPTION_KEY` مستقل و قوی قابل اجرا نیستند.
- JWT secret و کلید رمزنگاری باید حداقل ۳۲ نویسه، غیرضعیف و متفاوت باشند.
- CORS در production باید صریح، معتبر و غیر wildcard باشد.
- هر درخواست `X-Request-Id` دارد و پاسخ خطا path و requestId قابل رهگیری برمی‌گرداند.
- خطاهای شناخته‌شده Prisma بدون افشای جزئیات داخلی به 404 یا 409 نگاشت می‌شوند.
- PostgreSQL و API در Docker Compose به localhost bind شده‌اند.

### اعتبارسنجی ورودی و رابط وب

- DTOهای معتبر برای مخاطبین، حساب بانکی، queryهای pagination، انتشار محتوا و کپشن اجتماعی اضافه شده‌اند.
- boolean و عدد query قبل از ورود به Prisma به نوع صحیح تبدیل می‌شوند.
- hook دریافت داده refetch پایدار دارد؛ polling صفحات خبر و اجتماعی دیگر با هر render بازنشانی نمی‌شود.
- وابستگی‌های ناپایدار React، نقش‌های ARIA تب‌ها و importهای بدون استفاده اصلاح شدند.
- cache معیوب Next.js پاک و build کامل بازتولید شد؛ خطای `Cannot find module './819.js'` دیگر رخ نداد.

## قابلیت‌های درخواست‌شده که در ممیزی حاضر تأیید شدند

- «روتیتر» در کد runtime، مدل‌ها و رابط جاری وجود ندارد.
- WordPress زیرتب «پایش خبر» در تنظیمات نشر هوشمند است.
- گزینه «کانال‌های انتشار» در منوی نشر هوشمند نمایش داده نمی‌شود.
- صفحه مدیریت ماژول‌ها فقط فعال/غیرفعال‌سازی ماژول‌های مجاز را دارد و UI نصب یا به‌روزرسانی افزونه ندارد.
- تنظیمات شبکه‌های Telegram، Instagram، LinkedIn و Facebook همراه با تست اتصال مستقل وجود دارد.
- Telegram Worker URL دریافت می‌شود و درخواست آن از مسیر خروجی امن و بدون redirect ارسال می‌گردد.
- مودال انتشار اجتماعی ابتدا تصویر شاخص و کپشن را نشان می‌دهد؛ تولید تصویر قالبی فقط با اقدام کاربر انجام می‌شود.
- مدل‌های GapGPT به‌صورت فهرست دریافت می‌شوند و برای خلاصه خبر، ترجمه کامل، استودیوی اجتماعی و گزارش روزانه قابل انتخاب مستقل‌اند.
- گزارش روزانه امروز خبرها را می‌پذیرد، رد/افزودن خبر دارد، خروجی انگلیسی bullet-based تولید می‌کند و برای Outlook قابل کپی است.
- ساعت ۲۳ گزارش امروز آرشیو و گزارش روز بعد ساخته می‌شود.

## یافته‌های امنیتی مستقل و وضعیت اصلاح

| شدت | محل | یافته و وضعیت |
|---|---|---|
| P1 بالا | `source-reader.service.ts` | دورزدن SSRF با IPv4-mapped IPv6 و DNS rebinding ممکن بود؛ parsing، بررسی همه DNS answerها و socket pinning اصلاح و تست شد. |
| P2 متوسط | `auth.service.ts` | شمارش تلاش ناموفق ورود read-modify-write و قابل از‌دست‌رفتن بود؛ increment و lockout اتمیک و تست هم‌زمان اضافه شد. |
| P2 متوسط | `auth.service.ts` | reset token در دو درخواست هم‌زمان قابل مصرف بود؛ مصرف تک‌مرحله‌ای اتمیک و تست رقابتی اضافه شد. |

## کنترل‌های نهایی و نتایج واقعی

| کنترل | نتیجه |
|---|---|
| `pnpm -r typecheck` | موفق |
| `pnpm lint` | موفق؛ صفر error و ۹ warning تصویر پویا |
| `pnpm --filter @deska/api test` | ۲۳ از ۲۳ موفق |
| `pnpm build` | Shared، API و Web موفق؛ ۳۲ route |
| Prisma validate | schema معتبر |
| Prisma migrate diff | `No difference detected` |
| `pnpm db:audit` | ۲۹ جدول، صفر issue |
| `pnpm audit --prod` | صفر آسیب‌پذیری شناخته‌شده |
| PostgreSQL توسعه | container فعال روی localhost:5433 |
| API و Web توسعه | پورت‌های 3001 و 3000، Hot Reload فعال |
| `verify.ps1 -LiveOnly` | همه مراحل موفق |
| لاگ‌های stderr پس از smoke test | بدون خطای runtime |

## ریسک‌های باقی‌مانده و نقشه راه

### P0 ـ پیش از production

1. تمام secretهای production شامل PostgreSQL، JWT، encryption key، seed اولیه و integrationها را با مقادیر تصادفی مستقل بسازید؛ مقادیر توسعه reuse نشوند.
2. backup دیتابیس و volume فایل‌ها را restore کنید و RPO/RTO را در staging واقعاً اندازه بگیرید.
3. migrationها را روی clone ناشناس‌شده دیتابیس production تمرین و سپس smoke/E2E اجرا کنید.
4. اتصال واقعی GapGPT، WordPress، Telegram Worker، Instagram، LinkedIn و Facebook را با credential محدود و حساب آزمایشی کنترل کنید؛ این ممیزی secret واقعی را مصرف نکرده است.
5. egress firewall یا allowlist شبکه در سطح زیرساخت اعمال کنید تا کنترل SSRF اپلیکیشن تنها لایه دفاع نباشد.

### P1 ـ پایداری و مقیاس

1. scheduler و lock درون‌پردازه‌ای نشر را به queue و distributed lock مانند Redis/BullMQ منتقل کنید.
2. فایل‌ها و رسانه‌های نشر را برای چند replica به object storage سازگار با S3 منتقل کنید.
3. برای relationهای parent/child تجاری، unique و foreign key ترکیبی `(tenantId, id)` را تدریجی اضافه کنید؛ foreign key فعلی به Tenant و audit نرم‌افزاری دفاع خوبی است اما composite constraint دفاع عمیق‌تری می‌دهد.
4. E2E مرورگر برای login/refresh/logout، دعوت، ایجاد سازمان، تغییر نقش، آپلود سند، گزارش روزانه و انتشار اجتماعی به CI اضافه کنید.
5. audit trail رخدادهای حساس، metric، alert و tracing توزیع‌شده را تکمیل کنید.

### P2 ـ نگهداری و کارایی

1. سرویس‌های بزرگ `tenant.service.ts`، `publishing-settings.service.ts` و `source-reader.service.ts` را بر اساس مسئولیت به واحدهای کوچک‌تر تقسیم کنید.
2. ۹ هشدار `<img>` را با loader کنترل‌شده برای URL خارجی/Data URL یا component تصویر امن حل کنید؛ جایگزینی مستقیم فعلاً می‌تواند پیش‌نمایش‌های پویا را بشکند.
3. CSP production را با nonce/hash از وابستگی به `unsafe-inline` خارج کنید.
4. تنظیم seed منسوخ‌شونده `package.json#prisma` را هنگام ارتقا به Prisma 7 به `prisma.config.ts` منتقل کنید.
5. نگهداری access/refresh token در پاسخ JSON را در نسخه بعدی API با یک سیاست compatibility صریح محدود کنید؛ Web فعلی آن‌ها را ذخیره نمی‌کند.

## نتیجه اجرای انتشار نسخه ۱.۰.۲۵

طبق خروجی اجرای one-click deployment، export داده‌های مناسبت‌ها، افزایش نسخه، commit، push و ساخت imageهای API/Web انجام شده است؛ فقط گام `Deploy on server` شکست خورده است. جزئیات کامل log این گام از GitHub بدون دسترسی مدیریتی قابل دریافت نبود، اما در نسخه جدید متغیرهای `SETTINGS_ENCRYPTION_KEY` و `CORS_ORIGIN` در Compose اجباری شده‌اند، درحالی‌که نصب‌کننده قبلی کلید رمزنگاری را تولید نمی‌کرد. این ناسازگاری در نصب‌کننده‌های Linux/PowerShell اصلاح شد.

برای نصب موجود، پیش از اجرای مجدد انتشار باید در `.env` سرور وجود مقادیر `POSTGRES_PASSWORD`، `JWT_SECRET`، `SETTINGS_ENCRYPTION_KEY` و `CORS_ORIGIN` بررسی شود. workflow جدید پیش از pull این موارد را بدون نمایش مقدارشان کنترل و در صورت شکست، وضعیت container و لاگ محدود API/Web را چاپ می‌کند. اگر `SETTINGS_ENCRYPTION_KEY` قبلاً استفاده شده است، آن را تعویض نکنید؛ تغییر آن secretهای رمزنگاری‌شده را غیرقابل‌خواندن می‌کند.

## نتیجه نهایی

مشکلات بحرانی قابل اثبات این ممیزی اصلاح شده‌اند و مسیرهای build، migration، یکپارچگی tenant، امنیت نشست، کنترل خروجی شبکه، CI و اجرای محلی قابل تکرارند. سامانه در وضعیت فعلی برای staging مناسب است. production پس از انجام موارد P0 و تأیید integrationهای بیرونی و بازیابی بحران توصیه می‌شود.
