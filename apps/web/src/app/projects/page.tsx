'use client';

import { useState } from 'react';
import Link from 'next/link';
import { FolderKanban, ListTodo, Plus, ArrowLeft } from 'lucide-react';
import { ProtectedLayout } from '@/components/layout/protected-layout';
import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { useApi } from '@/hooks/use-api';
import { apiFetch } from '@/lib/utils';
import { formatPersianDigits } from '@deska/shared';

export default function ProjectsPage() { const { data, refetch } = useApi<any[]>('/projects'); const [name, setName] = useState(''); const [description, setDescription] = useState(''); const projects = Array.isArray(data) ? data : []; const add = async () => { if (!name.trim()) return; await apiFetch('/projects', { method: 'POST', body: { name: name.trim(), description: description.trim() } }); setName(''); setDescription(''); await refetch(); };
  return <ProtectedLayout title="پروژه‌ها"><main className="mx-auto w-full max-w-7xl space-y-6 p-4 sm:p-6" dir="rtl"><header className="flex flex-col gap-4 rounded-3xl bg-gradient-to-l from-slate-950 via-slate-900 to-indigo-950 p-6 text-white shadow-xl shadow-slate-900/10 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-4"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-white/10 ring-1 ring-white/15"><FolderKanban className="h-6 w-6" /></span><div><h1 className="text-2xl font-bold">مدیریت پروژه و تسک</h1><p className="mt-2 text-sm leading-6 text-slate-300">پروژه‌ها، اعضا، پیشرفت و کارهای سازمان را در یکجا مدیریت کنید.</p></div></div><span className="rounded-xl bg-white/10 px-4 py-2 text-sm">{formatPersianDigits(projects.length)} پروژه</span></header><Card className="p-5 sm:p-6"><div className="mb-4 flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-50 text-indigo-700"><Plus className="h-5 w-5" /></span><div><h2 className="font-bold text-slate-900">ایجاد پروژه جدید</h2><p className="mt-1 text-xs text-slate-500">نام و توضیح کوتاه پروژه را وارد کنید.</p></div></div><div className="grid gap-3 md:grid-cols-[1fr_2fr_auto]"><input className="rounded-xl border border-slate-300 px-3 py-2.5" placeholder="نام پروژه" value={name} onChange={(e) => setName(e.target.value)} /><input className="rounded-xl border border-slate-300 px-3 py-2.5" placeholder="توضیحات پروژه (اختیاری)" value={description} onChange={(e) => setDescription(e.target.value)} /><Button className="bg-indigo-600 hover:bg-indigo-700" onClick={add}><Plus className="h-4 w-4" /> افزودن پروژه</Button></div></Card><section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-bold text-slate-900">پروژه‌های سازمان</h2><span className="text-sm text-slate-500">{formatPersianDigits(projects.length)} مورد</span></div>{projects.length === 0 ? <Card className="flex min-h-48 flex-col items-center justify-center p-8 text-center"><FolderKanban className="h-9 w-9 text-slate-300" /><p className="mt-3 text-sm text-slate-500">هنوز پروژه‌ای ایجاد نشده است.</p></Card> : <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.map((project) => <Card className="p-5 transition hover:-translate-y-0.5 hover:shadow-md" key={project.id}><div className="flex items-start justify-between gap-3"><h3 className="font-bold text-slate-900">{project.name}</h3><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{project.status === 'active' ? 'فعال' : project.status}</span></div><p className="mt-3 min-h-12 text-sm leading-7 text-slate-500">{project.description || 'بدون توضیحات'}</p><div className="mt-4 flex items-center border-t border-slate-100 pt-4 text-sm text-slate-500"><span className="flex items-center gap-1.5"><ListTodo className="h-4 w-4" /> {formatPersianDigits(project._count?.tasks ?? 0)} تسک</span><Link className="mr-auto inline-flex items-center gap-1 font-semibold text-primary-700" href={`/projects/tasks?projectId=${project.id}`}>مشاهده <ArrowLeft className="h-4 w-4" /></Link></div></Card>)}</div>}</section></main></ProtectedLayout>; }
